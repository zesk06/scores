language: python
python:
  - "2.7"
# scores code is not 3.5 compatible :(
#  - "3.5"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: py.test --cov=scores
